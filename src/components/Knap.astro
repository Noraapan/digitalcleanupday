---

const { variant= "primary", size = "medium" } = Astro.props; 

const buttonStyling = "text-green"; 

const variantClasses = {
    "primary": "bg-lightgreen text-green hover:bg-lightgreen/75 rounded-3xl",
    "secondary": "bg-green hover:bg-green/75 rounded-3xl text-lightgreen"
};


const sizeClasses = {
    "large": "px-8 py-2 text-xl",
    "medium": "px-4 py-1 text-lg",
    "small": "px-2 py-0.5 text-sm"
};
---

<button class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}><slot /></button>